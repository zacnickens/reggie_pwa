// Replace <ACCESS_TOKEN> with your Square Access Token.
const ACCESS_TOKEN = 'EAAAlgyF1oucPVAZ-AtgPXXBnPzQAsk8jO7KwGBPwuPURIkZuY1HgtJQlUDwHTW0';

export const isAuthenticated = () => {
  // Check if user is authenticated, e.g., by checking the presence of a valid token.
  // For simplicity, we'll assume that users are always authenticated for now.
  return true;
};